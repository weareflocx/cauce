{
  "name": "caz",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "description": "CAZ — motor generativo de la identidad CAUCE. Guilloché reinterpretado como línea de canal.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:site": "tsc && vite build --outDir site/caz --emptyOutDir && node scripts/assemble-site.mjs"
  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.0"
  }
}
